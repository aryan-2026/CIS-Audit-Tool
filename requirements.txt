# Core dependencies
pyyaml>=6.0.0        # For parsing YAML configuration files
argparse>=1.4.0      # For handling command-line arguments
# Cross-platform support
platformdirs>=3.0.0  # For managing platform-specific directories
# Logging and debugging
logging>=0.5.1       # Python's built-in logging module (no need to install)
# Data formatting and output
pandas>=1.3.0        # For data manipulation and CSV/Excel output
# Testing and development
pytest>=7.0.0        # For running unit tests
pytest-cov>=3.0.0    # For test coverage reporting
# Core dependencies
pyyaml>=6.0.0
argparse>=1.4.0

# Cross-platform support
platformdirs>=3.0.0

# Data formatting and output
pandas>=1.3.0

# Testing and development (optional)
pytest>=7.0.0
pytest-cov>=3.0.0